package ship.developer;

public class Main {
    public static void main(String[] args) {
        String line = "3.\tImplement your own binary tree generic collection that allows to add elements (other operations are optional)";
        Task1.getLengthOfWords(line, true);
    }
}
